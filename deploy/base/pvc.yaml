apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: skillctl-catalog-data
  labels:
    app.kubernetes.io/name: skillctl-catalog
    app.kubernetes.io/component: server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
