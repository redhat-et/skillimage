apiVersion: v1
kind: Service
metadata:
  name: skillctl-catalog
  labels:
    app.kubernetes.io/name: skillctl-catalog
    app.kubernetes.io/component: server
spec:
  selector:
    app.kubernetes.io/name: skillctl-catalog
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
